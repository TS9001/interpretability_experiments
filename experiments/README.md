# Experiments

This directory contains different interpretability experiments for AI/ML models.

## Structure

Each subdirectory should contain a separate experiment with its own:
- Code files
- Documentation
- Results (if any)

## Example

See `example_experiment/` for a template structure.
